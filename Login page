<?php
 $host="localhost";
 $user="root";
 $password="";
 $db="pett";
  
 Mysql_connect($host ,$user ,$password);
 $mysql_select_db($db);
  
 if(isset([<'nom d'utilisateur']){
    $uname=$_POST['nom d'utilisateur'];
    $password=$_POST['mot de passe'];
    $sql="select * from login where user='" .$uname."' AND passwod='".$password."' limit 1";
    $result=mysql_query($sql);
    }

 if( mysql_num_rows($result)==1){
      echo "you have successfully login";
      exit();
      }
   
  else{
      echo "you have entered incorrect password";
  }

?>

<!DOCTYPE html>
<html>
<head>
   
 <title>PetMe</title>
</head>
<body>    
  <h1> bienvenue sur PetMe </h1>
 <div id="box">
<form method="POST" action="#">
   <div class="form_input">
          <Input type="text" name="nom d'utilisateur" placeholder="saisir le nom d'utilisateur" />
    </div>
    <div class="form_input">
        <Input type="password" name="mot de passe" placeholder="saisir le mot de passe" />
    </div>
    <Input type="submit" name="submit" value="Se connecter" class="btn-login" />
 </form>
 </div>
 </body>
 </html>
